<div class="row">
  <aside class="col-sm-4">
    <h2>#MY PAGE</h2>
    <div class="card">
      <div class="card-header">
        <h3 class="card-title"><%= @user.name %></h3>
      </div>
      <div class="card-body">
      <%= link_to "LE'TS POST!!", '/posts/new', class: "btn btn-secondary" %>
      </div>
    </div>
 </aside>
  <div class="col-sm-8">
    <ul class="nav nav-tabs nav-justified mb-3">
      <li class="nav-item"><a href="<%= user_path(@user) %>" class="nav-link <%= 'active' if current_page?(user_path(@user)) %>">MY STYLE</a></li>
    </ul>
  </div>
</div>
